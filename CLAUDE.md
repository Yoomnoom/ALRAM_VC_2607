# CLAUDE.md

이 파일은 이 프로젝트 전체에서 항상 지켜야 할 공통 규칙을 담는다.
기능별 세부 규칙은 각 기능 폴더의 CLAUDE.md를 따로 참고한다.

## 🚨 최우선 보안 규칙: API 키/시크릿 노출 금지

**네이버 API 키(Client ID/Secret) 등 모든 외부 API 키·시크릿은 프론트엔드 코드(HTML, JS, CSS, 클라이언트 번들)에 절대 넣지 않는다.**

- API 키는 반드시 서버 사이드(예: Supabase Edge Function 등)의 환경변수/시크릿으로만 관리한다.
- 외부 API 호출은 반드시 서버(프록시)를 경유하며, 클라이언트는 프록시 엔드포인트만 호출한다.
- 코드, 커밋, 저장소 어디에도 키 값이 하드코딩되거나 커밋되지 않도록 항상 확인한다.
- 이 규칙은 어떤 편의나 임시 구현을 이유로도 예외를 두지 않는다.

## 데이터 저장 원칙

- 외부 API로 조회하는 데이터(예: 뉴스)는 DB 등 영구 저장소에 저장하지 않고, 매 요청마다 실시간으로 조회한다.
- 클라이언트 측 당일 캐시(localStorage 등) 정도는 허용되나, 서버/DB에 데이터를 영구 적재하는 구조는 만들지 않는다.

### 예외: Google Sheets에 기록하는 항목

아래 두 가지만 예외적으로 Google Apps Script 웹 앱을 통해 Google Sheets에 기록한다. 그 외 외부 API 조회 데이터는 여전히 위 원칙(영구 저장 금지)이 그대로 적용된다.

- 알람 시 음성으로 읽어준 **뉴스 브리핑 내용** — 알람이 울릴 때마다 자동으로 기록되며(BriefingLog 시트), 사용자가 "지금 저장"을 누르면 각 알람이 마지막으로 울렸을 때 읽어준 브리핑 내용이 해당 알람 행의 "내용" 컬럼에도 함께 저장된다(Alarms 시트).
- **최근뉴스 목록에서 사용자가 직접 체크한 기사** — 사용자가 최근뉴스 카드의 체크박스로 선택한 기사만, "지금 저장"을 누른 시점에 예외적으로 기록한다(SavedNews 시트). 체크하지 않은 기사나 목록 조회 자체는 여전히 저장하지 않는다.

## 실패 허용 원칙 (Graceful Degradation)

- 알람 소리처럼 핵심 기능은 부가 기능(예: 뉴스 브리핑, TTS 등)의 실패와 완전히 독립적으로 동작해야 한다.
- **부가 기능이 실패해도 핵심 기능(알람 소리 재생)은 반드시 정상적으로 동작해야 한다.**
- 부가 기능의 예외/에러가 핵심 기능의 흐름을 막거나 지연시켜서는 안 되며, 항상 핵심 기능과 분리된 try/catch·독립 실행 흐름으로 구현한다.

## 작업 순서 원칙

새 기능을 구현할 때는 항상 아래 순서를 따른다.

1. 먼저 가짜 데이터(mock data)로 화면(UI)을 완성한다.
2. 그다음 실제 백엔드/외부 API 연결을 붙인다.
3. 예외 처리(에러 핸들링, 실패 시 폴백 등)는 가장 마지막에 추가한다.

이 순서를 건너뛰거나 뒤바꾸지 않는다.

## 미확정 사항 임의 결정 금지

- 기능 스펙 중 사용자가 아직 명시적으로 결정하지 않은 사항은 임의로 결정해서 구현하지 말고, 반드시 사용자에게 먼저 확인한다.
- (참고) 뉴스 검색 키워드 방식과 알람음/뉴스 브리핑 재생 타이밍은 이미 확정되어 `news/prd.md`에 반영되어 있다. 이 규칙은 앞으로 새롭게 생기는 미확정 항목에 적용된다.

---

뉴스 기능의 세부 규칙(파일 구조, 함수 이름 규칙)은 news/CLAUDE.md를 참고할 것.
