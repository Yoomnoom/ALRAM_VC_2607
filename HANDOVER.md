# 인수인계 문서

세션이 끝날 때마다 맨 위에 새 항목을 추가한다. 오래된 기록도 지우지 않고 누적한다.

---

## 2026-07-29 — PWA 전환 + 하단 탭 UI 전면 개편

### 이번에 한 일

**1. PWA 전환**
- `manifest.json`, `sw.js` 추가, `icons/` 폴더에 기존 알람시계 이미지를 리사이즈한 192/512/180px 아이콘 생성
- `sw.js`는 앱 셸(html/css/js/manifest/아이콘)만 캐싱하고, 뉴스·날씨 등 실시간 API 요청은 절대 가로채지 않도록 캐시 대상 URL을 화이트리스트로 제한 (`SHELL_URLS`)
- 캐시 버전(`CACHE_NAME`)은 앱 셸 파일이 바뀔 때마다 값을 올려야 사용자 브라우저가 새 버전을 받는다 (현재 `alarm-app-shell-v13`). **다음 세션에서 index.html/style.css/script.js/news.js/news.css 중 하나라도 바꾸면 이 값을 반드시 올릴 것** — 안 올리면 사용자가 새로고침해도 예전 화면이 나온다 (이번 세션에 실제로 겪은 문제).

**2. 하단 탭 네비게이션 (알람 / 뉴스 / 설정)**
- 기존에는 "최근뉴스" 버튼을 눌러 오버레이 패널을 여는 방식이었는데, 하단 고정 탭 3개로 전환
- 검색+키워드1+키워드2로 나뉘어 있던 화면을 "뉴스" 탭 하나로 합침: 데스크톱은 2컬럼 나란히, 모바일은 상단 전환 버튼(스타트업/웹툰)으로 1컬럼씩
- 알람 탭 하단의 "📰 최근뉴스" 버튼 + 새로고침 아이콘은 **원래 디자인 그대로 유지** (사용자가 중간에 "처음 모습대로 되돌려달라"고 요청). 새로고침 아이콘을 눌러도 탭 전환 + 강제 새로고침이 함께 일어남
- 설정 화면은 알람 카드와 같은 크기(460px, `#tabPage-settings .app`로 스코프 지정)를 쓰고, 뉴스 화면(920px)과는 별개 크기

**3. 설정 탭 신규 구현**
- 다크모드 토글 (localStorage `darkModeEnabled`, `body.dark-theme` 클래스로 전체 앱 스타일 override)
- 글자 크기 작게/보통/크게 (localStorage `fontScale`, `document.body.style.zoom`으로 구현 — **Firefox는 zoom 속성 미지원이라 이 브라우저에선 효과 없음**, Chrome/Edge 계열만 정상 동작)
- 데이터 초기화 버튼 (localStorage의 `newsKeywords`/`newsCache`만 삭제, 알람 데이터는 안 건드림)
- 다크모드에서 날씨 배경도 조건별로 별도 다크 그라디언트를 넣어서, 어두운 테마에서도 비/이슬비 등 날씨별 색상 구분이 유지되도록 함 (처음엔 배경 전체를 하나의 색으로 덮어써서 구분이 안 되는 버그가 있었고, 조건별 override로 수정함)

**4. 네이티브 alert()/confirm() 제거**
- 브라우저 기본 alert/confirm 다이얼로그가 앱 디자인과 안 어울린다는 피드백을 받아, `#appModalOverlay` 커스텀 모달로 전부 교체 (`showAppAlert(message)` / `showAppConfirm(message, {showCancel})`, 둘 다 Promise 반환)
- 알람 시간/요일/시작일 미선택 경고, 데이터 초기화 확인 총 4곳 교체 완료. 네이티브 다이얼로그가 하나도 안 뜨는 것까지 Playwright로 확인함

**5. 알람 편집 중 이탈 시 자동 취소**
- 알람 추가/시간선택 오버레이가 열린 상태에서 배경(어두운 영역) 클릭하거나 다른 탭으로 이동하면, "취소" 버튼을 누른 것과 동일하게 처리되고 입력값이 초기화되도록 함 (`cancelAddAlarm()`, `closeTimePicker()`를 `switchTab()` 안에서도 호출)

**6. 모바일 반응형**
- 좁은 화면(≤600px)에서는 카드가 화면을 거의 꽉 채우되, 좌우로 14px 여백을 남겨서 날씨 배경이 살짝 비치도록 함 (완전히 꽉 채우면 배경이 하나도 안 보인다는 피드백 반영)

### 세션 막바지에 발견해서 고친 버그 (일반 리뷰 에이전트로 diff 검수)
1. **[심각]** 하단 탭 바(z-index 20)가 알람 울림 화면(`.ringing-overlay`, 기존 z-index 10)보다 위에 떠 있어서, 알람이 울리는 중에도 하단 탭을 눌러 다른 화면으로 이동할 수 있었음 → `.ringing-overlay` z-index를 25로 올려서 알람 울리는 동안은 탭 전환이 아예 안 먹히도록 수정. (알람 추가/시간선택 오버레이는 반대로 **의도적으로** 탭 바보다 아래(z-index 15/16)에 둬서, 탭을 눌러 이동하면 그게 곧 취소 처리가 되도록 한 것 — 이건 버그 아니고 5번 기능을 위한 설계임)
2. **[높음]** 지정 키워드가 0개일 때 뉴스 탭에 들어가면 `renderNewsRefreshedAt(null, "")`가 `date.getHours()`를 호출해서 예외가 났음 (`failMessage`가 빈 문자열이면 falsy라서 date 분기로 빠짐) → `typeof failMessage === "string"`으로 체크하도록 수정
3. **[중간]** 커스텀 alert/confirm 모달을 연달아 빠르게 여러 번 띄우면 각 호출마다 버튼에 이벤트 리스너를 새로 붙이기만 하고 안 지워서, 확인 버튼 한 번 클릭에 여러 Promise가 한꺼번에 resolve될 수 있었음 → 리스너를 모듈 로드 시 한 번만 바인딩하고 "현재 대기 중인 resolve" 하나만 추적하는 방식으로 리팩터링
4. **[낮음]** `news.js`의 `runNewsRefresh` 함수명이 `news/CLAUDE.md`의 `fetchXxx`/`renderXxx`/`buildXxx` 네이밍 규칙과 안 맞아서 `fetchNewsRefresh`로 개명

이 4개 모두 Playwright로 재현 → 수정 → 재검증까지 마쳤음.

### 확인한 것 (회귀 없음)
- 알람 CRUD(추가/수정/토글/삭제), 알람 울림→스누즈/끄기, 알람 편집 화면 시간 미리채움 — 전부 이번 리팩토링 이후에도 정상
- PWA manifest.json 문법, service worker 등록·activate 정상
- 라이트/다크 모드, 데스크톱/모바일 레이아웃 모두 스크린샷으로 시각 확인

### 알고 있지만 손 안 댄 것 (의도적으로 그대로 둠)
- **뉴스 브리핑 Google Sheets 로그 실패**: `logBriefingToSheet()`가 CORS 에러로 항상 실패하는 게 콘솔에 찍힘 (`script.google.com`이 preflight에 CORS 헤더를 안 줌). 이번 세션 이전부터 있던 문제로 보이고, try/catch로 감싸져 있어 알람 소리 자체엔 영향 없음 (실패 허용 원칙 준수). Apps Script 배포 설정(웹앱 접근 권한 등) 쪽을 봐야 할 수도 있음 — 다음에 시간 나면 확인 필요.
- **TTS 음성 속도/목소리 선택**: `news/prd.md`의 Open Question으로 여전히 미정. 임의로 구현 안 함.
- 알람 소리 관련 파일(`.env`, `개인메모/`)은 이번 세션에서 열어보긴 했으나(사용자가 IDE에 띄워둠) 문서/커밋에 전혀 포함하지 않음 — `개인메모/`엔 Supabase 액세스 토큰 등 실제 시크릿이 있으니 절대 커밋되지 않게 주의할 것 (`.gitignore`에 이미 등록되어 있음, 확인 완료).

### 실행·테스트 방법
- 빌드 없음. 정적 파일이라 `npx serve .` 등으로 로컬 서버 띄우면 됨 (README.md 참고). `file://`로 열면 fetch/서비스워커가 아예 동작 안 함.
- 로컬에서 실제 뉴스 API를 테스트하려면 `config.js`가 필요 (gitignored). `config.example.js` 참고해서 직접 채우거나 `node generate-config.js` 사용.
- 서비스워커 캐시 때문에 변경사항이 바로 안 보이면: 새로고침을 **두 번** (한 번은 새 SW 설치, 두 번째에 적용됨).

### 다음에 이어서 할 일 후보
- Google Apps Script CORS 문제 원인 파악 (배포를 "웹앱, 모든 사용자 액세스"로 다시 확인해볼 것)
- TTS 음성 속도/목소리 설정 여부는 사용자에게 먼저 확인 후 진행
- 글자 크기 조절(`zoom`)이 Firefox에서 안 먹히는 점 — 크로스브라우저 대응이 필요하다고 판단되면 rem 기반 리팩터링 별도 논의
